import * as mmdb from 'maxmind';
import ReaderModel from './readerModel';
export default class Reader {
    static open(file: string, opts?: mmdb.OpenOpts): Promise<ReaderModel>;
    static openBuffer(buffer: Buffer): ReaderModel;
}
